.DS_Store
.claude/serve.js

# PDFs and project backups that the apps generate land in whatever folder
# the browser downloads to, which during local testing is this one. Only
# ignore the repo root - ot/templates/ holds the real blank OT form and
# must stay tracked.
/*.pdf
/*.json
!/manifest.json
